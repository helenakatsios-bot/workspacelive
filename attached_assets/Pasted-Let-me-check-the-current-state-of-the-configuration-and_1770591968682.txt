Let me check the current state of the configuration and test the published site:

The port config is now correct with just one mapping. But the published site is still 404. Let me dig deeper - this might be a build or startup issue in the deployment itself. Let me check a few things:

I see that process.env.NODE_ENV is already hardcoded to "production" during the build step, so that wasn't the issue. Let me investigate if the server is crashing on startup in the deployment - maybe a missing environment variable or dependency is causing the crash:

Let me check routes.ts to see the startup sequence - I suspect the auth setup might be blocking the server from starting:

I think the issue might be the server crashing during startup in production. Let me simulate exactly what the deployment does and capture any errors:

The server starts perfectly when I run it locally. The build and server are fine. Let me check if there might be an issue with the deployment build step - specifically if devDependencies (like tsx which is used in the build script) are available during deployment:

The docs mention there's a "Logs" tab in the Publishing section. But let me try something else first - the OIDC authentication setup might be blocking the server from starting up in time for the deployment health checks. Let me make the server start listening immediately and do the auth setup in the background:

Let me try a different approach. I'll remove the secondary port listener (no longer needed since the extra port mapping is gone), simplify the deployment config, and rebuild:

Now let me rebuild:

Now let me verify the simplified build works:

Server starts cleanly. Now let me check one more thing - the actual deployment URL might be different from what we're testing. Let me also check if there's something in the RESEND configuration or startup that could crash:

The code and build are working correctly - the issue is something specific to the Replit deployment infrastructure. I need your help to diagnose this further.

Could you check the deployment logs? Here's how:

Click the "Publishing" tab at the top of your workspace
Look for a "Logs" tab or "View publish logs" option
Check if there are any error messages
This will show us whether the app is actually starting in the deployed environment or if it's crashing with an error. That will tell us exactly what to fix.

Also, try clicking "Republish" one more time - I've simplified the server code by removing the unnecessary secondary port listener. Sometimes the deployment needs a fresh publish after configuration changes to fully take effect.

If you can share the deployment logs, I'll be able to pinpoint exactly what's happening and fix it right away.